# P3394 Agent v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Upgrade the existing AgentClaw-native P3394 Runtime Agent so its manifest, UMF normalization, session lifecycle, relationship/capability authorization, audit records, deterministic protocol commands, and agentic LLM execution are covered by tests.

**Architecture:** Keep the existing copied `p3394_runtime_agent` template under AgentClaw. Extend the ingress node with P3394 v0.9.0-inspired manifest fields, deterministic protocol command handlers, in-memory session/audit stores, and conformance reporting while preserving the existing `SmartPreFilterNode -> LLMNode(agent_style="agentic")` execution path.

**Tech Stack:** Python 3.11, AgentClaw workflow nodes, pytest, Vue/Vitest dashboard tests.

---

### Task 1: Protocol Surface Tests

**Files:**
- Modify: `agentclaw/test/unit/test_example_agent_square_templates.py`
- Modify: `agentclaw/agent_square/p3394_runtime_agent/agents/p3394_runtime_agent.py`

- [ ] Write failing tests for the complete v2 protocol surface: `manifest.describe`, `message.normalize`, `session.create`, `session.fetch`, `session.close`, `audit.summary`, `conformance.check`.
- [ ] Run the targeted pytest file and confirm the new tests fail on missing or incomplete behavior.
- [ ] Implement the minimal runtime changes needed to pass those tests.
- [ ] Re-run the targeted pytest file until it passes.

### Task 2: Agentic Execution Preservation

**Files:**
- Modify: `agentclaw/test/unit/test_example_agent_square_templates.py`
- Modify: `agentclaw/agent_square/p3394_runtime_agent/agents/p3394_runtime_agent.py`

- [ ] Test that normal user requests still route through `p3394_init -> smart_prefilter -> agent`.
- [ ] Test that the `LLMNode` remains `agent_style="agentic"` with built-in tools and skills enabled.
- [ ] Keep the existing execution chain unchanged while expanding the P3394 protocol layer.

### Task 3: Documentation and Verification

**Files:**
- Modify: `agentclaw/agent_square/p3394_runtime_agent/README.md`

- [ ] Update README examples to include `session.fetch`, `session.close`, and `conformance.check`.
- [ ] Run Python unit tests for the P3394 agent and model fallback.
- [ ] Run the dashboard Vitest suite that covers the P3394 navigation entry.
- [ ] Perform the completion audit against every explicit user requirement.
